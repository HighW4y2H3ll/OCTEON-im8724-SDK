/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-ntradbigmips)
GROUP ( /lib32-fp/libpthread.so.0 /usr/lib32-fp/libpthread_nonshared.a )
